import type { Rib } from "@keelson/shared";

// Scaffold identity only. The genesis / rooms / lenses design lives in
// docs/PRD.md and docs/ARCHITECTURE.md; hooks land per the phase plan there.
const rib: Rib = {
  id: "chamber",
  displayName: "Chamber",
};

export default rib;
